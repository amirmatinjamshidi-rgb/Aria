{
  "name": "tiny-core-kata",
  "private": true,
  "version": "0.1.0",
  "description": "Learning topic 1 practice: rebuild a tiny @aria/core (container + bus) from scratch, test-driven",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
